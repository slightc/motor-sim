# 真机验证报告（模板）— ihm07m1_foc

> 每次上板验证**复制本模板**填写，存为
> `data/real/validation/<日期>_<电机>_<固件版本>.md`，原始录波另存 CSV
> （字段见 `docs/05_hardware_deployment.md` §3.1）。指标/容差对齐 docs/05 §4.2。
>
> 规则：**未填 = 未测**；失败项照实记录（失败样本对 core 标定同样有价值，agent.md）。
> 先 `bash test/run_host_test.sh` 全绿、`pio run` 通过，再上板——主机回归是上板的前置门槛。

---

## 0. 元信息（provenance，可复现的前提）

- 日期 / 操作人：
- 固件版本：`git rev-parse --short HEAD` = `____`；构建 env=`nucleo_f302r8`
- 主机回归：`bash test/run_host_test.sh` = **PASS / FAIL**（必须 PASS 才上板）
- 电机：型号 / 序列号、极对数 `p`、额定 V/I/转速、铭牌 R / L / Ke（若有）
- 板子：X-NUCLEO-IHM07M1 序列号、分流跳线 J5/J6（单/三分流）、母线电压实测 ___V
- 仪器：示波器、电流钳、台架转矩、**参考编码器 PPR**（做近似真值用）、室温 ___℃
- BSP 核对：引脚映射（PA8/9/10、PC10/11/12、电流 ADC 通道）对照 UM1943 = **OK / 差异 ___**
- `BSP_MOTOR_POLEPAIRS` = ___ 是否等于真实 `p`：

## 1. 上电与安全（docs/05 §2.3）

- [ ] 零电流校准后三相 ADC 偏置(counts)：U `___` V `___` W `___`（应≈半量程）
- [ ] EN 使能后无异响 / 过流；静态母线电流 `___A`
- [ ] 脱开负载；`BSP_I_MAX`=2.5A 软限流生效；堵转 / 过温保护：`___`

## 2. 参数自整定（MODE_PARAM_ID，静止）

| 量 | 实测 | 重复性(n=__) | 参考(铭牌/datasheet) | 偏差 | 合理? |
|----|------|--------------|----------------------|------|-------|
| Rs (Ω)  | | ± | | | |
| Ld (mH) | | ± | | | |
| Lq (mH) | | ± | | | |
| ψ (Wb)  旋转段估 | | | (由 Ke 换算) | | |

- 物理合理性自检：`Rs>0`、`Lq≥Ld`（典型凸极）、`Ld/Lq` 量级与铭牌一致。
- 注入波形（可选）：注入电压 / 电流纹波截图或 CSV 链接 `____`。
- 判定：测得 Rs/Lq 是否可直接喂无感观测器（observer 仅需 R/L）。**PASS / FAIL：**

## 3. 开环 I/f（MODE_OPENLOOP_IF）

- 相序 / 转向：**正确 / 需对调**（交换任意两相，或改 `BSP_CURR_SIGN`/编码器方向）
- 起转最低稳定 ω：`___ rad/s`；注入电流 `___A`
- 现象 / 异常：

## 4. 有感闭环 FOC（MODE_SENSORED，需编码器）

- 速度阶跃 30→50 rad/s：上升时间 `__ms`、超调 `__%`、稳态误差 `__rad/s`
- 电流环：iq 阶跃上升时间 / 超调；`id≈0` 维持情况
- **vs 仿真同工况**（control/01）：上升时间 / 超调差 `__%`（容差 <10%，§4.2）

## 5. 无感闭环 FOC（MODE_SENSORLESS）—— 本次需求重点

> 真机无真值；评估用**编码器读数作近似真值**（docs/05 §2.1）。无感不消费它，仅记录比对。

- I/f→观测器交接：成功交接电速 `___rad/s`、交接电流尖峰 `___A`、是否失步
- 稳态位置误差 `|θ_est − θ_enc|`：RMS `__°(电)`、峰 `__°`（容差 <1° 电，§4.2）
- 估计速度 vs 编码器速度：误差 `__%`
- 最低可维持无感转速：`___rad/s`（低于此反电动势弱、失锁——预期边界）
- 负载阶跃（`__N·m`）：是否保持锁定、位置误差变化
- ψ-无关验证：确认观测器仅用 Rs/Lq 即锁（佐证 PLL 按 |e| 归一化的结论）
- 判定：**PASS / FAIL：**

## 6. 仿真↔实测对齐（docs/05 §4.2）

用**真机的输入序列**驱动仿真，逐点比对（这一步把"验证"升级为"可回归"）：

| 指标 | 定义 | 容差 | 实测 | 判定 |
|------|------|------|------|------|
| 电流 RMSE | `rms(i_dq_sim − i_dq_real)` | <5% 额定 | | |
| 角度偏差 | `|θ_sim − θ_real|` 稳态 | <1°(电) | | |
| 速度阶跃 | 上升 / 超调差 | <10% | | |

- 偏差归因：**参数不准**（调 MotorConfig）/ **模型缺现象**（补 core `_deriv`，向后兼容）——docs/05 §3.3。

## 7. 结论与回馈

- 总判定：**PASS / 有条件 PASS / FAIL**
- 暴露的 core 短板（按 agent.md「core 反哺」五维度：精准/准确/可扩展/功能/生态）：
- 下一步：标定回填 `MotorConfig` 预设 / 扩充 golden set / 改 core 物理项 / 调固件
